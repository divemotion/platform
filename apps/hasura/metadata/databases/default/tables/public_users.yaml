table:
  schema: public
  name: users
insert_permissions:
  - role: user_admin
    permission:
      check: {}
      columns:
        - email
        - first_name
        - last_name
        - created_at
        - updated_at
        - id
      backend_only: false
select_permissions:
  - role: user_admin
    permission:
      columns:
        - email
        - first_name
        - last_name
        - created_at
        - updated_at
        - id
      filter: {}
      limit: 100
update_permissions:
  - role: user_admin
    permission:
      columns:
        - email
        - first_name
        - last_name
        - created_at
        - updated_at
        - id
      filter: {}
      check: {}
delete_permissions:
  - role: user_admin
    permission:
      filter: {}
